# addons

World of Warcraft addons, written in Lua. Each addon lives in its own subfolder (named to match its `.toc` file) so it can be symlinked or packaged independently, e.g.:

```
addons/
  MyAddon/
    MyAddon.toc
    MyAddon.lua
```
